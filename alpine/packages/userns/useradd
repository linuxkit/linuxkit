#!/bin/sh

for a in $*
do
        [ "$a" = "-N" ] && a=""
        [ "$a" = "-M" ] && a="-H"
        [ "$a" = "-g" ] && a="-G"
        args="$args $a"
done

echo $args

exec /usr/sbin/adduser -D ${args}
